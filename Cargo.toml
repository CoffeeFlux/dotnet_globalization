[package]
name = "dotnet_globalization"
version = "0.1.0"
edition = "2018"

[package.metadata.cargo-all-features]
skip_optional_dependencies = true

[lib]
crate-type = ["staticlib"]

[dependencies]
icu = "0.1.0"
icu_provider_fs = "0.1"
